apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "essies-emoji-counter.fullname" . }}-test-connection"
  labels:
    {{- include "essies-emoji-counter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "essies-emoji-counter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
