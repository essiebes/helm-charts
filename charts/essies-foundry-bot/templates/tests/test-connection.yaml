apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "essies-foundry-bot.fullname" . }}-test-connection"
  labels:
    {{- include "essies-foundry-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "essies-foundry-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
